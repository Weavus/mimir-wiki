Task bundle: operational

Extract operational enrichment fields from the supplied Confluence document
chunk. Return one JSON object that includes only the requested fields. Keep
warnings concise and ground every entity or signal in the text.

Return only JSON matching this combined shape:
{contract}

Metadata JSON:
{metadata_json}

Document text chunk:
{chunk}
