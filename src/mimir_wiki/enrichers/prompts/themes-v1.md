Task: themes

Extract broad themes from the supplied Confluence document chunk. Themes should
be reusable corpus-level groupings, not one-off page titles.

Return only JSON matching this shape:
{contract}

Metadata JSON:
{metadata_json}

Document text chunk:
{chunk}
