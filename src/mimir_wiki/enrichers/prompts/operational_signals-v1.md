Task: operational_signals

Detect operational support signals in the supplied Confluence document chunk.
Return true only when the signal is directly supported by the text.

Return only JSON matching this shape:
{contract}

Metadata JSON:
{metadata_json}

Document text chunk:
{chunk}
